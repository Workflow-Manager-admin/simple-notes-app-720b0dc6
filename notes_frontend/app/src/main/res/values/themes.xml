<resources>
    <!-- Minimal XML theme for Compose Material3 app; backed by AppCompat for compatibility -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight">
        <item name="android:windowBackground">@color/background</item>
    </style>
</resources>
